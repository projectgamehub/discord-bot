import ping from "./ping.js";
import server from "./server.js";
import user from "./user.js";
import reload from "./reload.js";

const commands = [ping, server, user, reload];

export default commands;
